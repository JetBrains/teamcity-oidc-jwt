<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>jwt</artifactId>
    <groupId>org.jetbrains.teamcity.builds</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>build</artifactId>
  <packaging>pom</packaging>
  <properties>
    <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
    <teamcity.jwt.plugin.version>snapshot-${maven.build.timestamp}</teamcity.jwt.plugin.version>
    <teamcity.jwt.plugin.vendorName>JetBrains</teamcity.jwt.plugin.vendorName>
    <teamcity.jwt.plugin.vendorUrl>https://www.jetbrains.com/</teamcity.jwt.plugin.vendorUrl>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.jetbrains.teamcity.builds</groupId>
      <artifactId>server</artifactId>
      <version>1.0-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.jetbrains.teamcity.builds</groupId>
      <artifactId>gcp-signer</artifactId>
      <version>1.0-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>com.google.code.maven-replacer-plugin</groupId>
        <artifactId>replacer</artifactId>
        <version>1.5.2</version>
        <executions>
          <execution>
            <id>replace-server</id>
            <phase>process-sources</phase>
            <goals>
              <goal>replace</goal>
            </goals>
            <configuration>
              <file>${basedir}/../server/teamcity-plugin.xml</file>
              <outputFile>${basedir}/target/server/teamcity-plugin.xml</outputFile>
            </configuration>
          </execution>
          <execution>
            <id>replace-gcp</id>
            <phase>process-sources</phase>
            <goals>
              <goal>replace</goal>
            </goals>
            <configuration>
              <file>${basedir}/../gcp-signer/teamcity-plugin.xml</file>
              <outputFile>${basedir}/target/gcp-signer/teamcity-plugin.xml</outputFile>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <replacements>
            <replacement>
              <token>@Version@</token>
              <value>${teamcity.jwt.plugin.version}</value>
            </replacement>
            <replacement>
              <token>@VendorName@</token>
              <value>${teamcity.jwt.plugin.vendorName}</value>
            </replacement>
            <replacement>
              <token>@VendorURL@</token>
              <value>${teamcity.jwt.plugin.vendorUrl}</value>
            </replacement>
          </replacements>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.8.0</version>
        <executions>
          <execution>
            <id>make-assembly-server</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>oidc-jwt</finalName>
              <outputDirectory>${project.parent.build.directory}</outputDirectory>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>server-plugin-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
          <execution>
            <id>make-assembly-gcp</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>oidc-signer-gcp</finalName>
              <outputDirectory>${project.parent.build.directory}</outputDirectory>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>gcp-plugin-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
